import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should build', (WidgetTester tester) async {
    // Placeholder test — will be expanded once features are built.
    expect(1 + 1, equals(2));
  });
}
